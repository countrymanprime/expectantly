<!-- markdownlint-disable-file MD041 -->
## Summary

<!-- What does this change do, and why? Link the issue. -->

## Changes

-

## Documentation

<!-- Tick what applies. Put "N/A" and a one-line reason next to anything that doesn't. -->

- [ ] Public API changes have accurate XML docs (`<summary>`, `<param>`, `<returns>`, `<exception>`)
- [ ] README / `docs/` updated for changed behavior, configuration, or commands
- [ ] Mermaid diagrams updated if components, flows, states, or the data model changed
- [ ] ADR added (or superseded) for a significant decision
- [ ] CHANGELOG entry added (breaking changes marked **Breaking** and a migration note linked)
- [ ] Generated docs regenerated (snippets, ERD, `PublicAPI.Unshipped.txt`), if the repo uses them

## Testing

<!-- How was this verified? Commands run, new tests, manual checks. -->
